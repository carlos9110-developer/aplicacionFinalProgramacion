import { MurcielagoPipe } from './murcielago.pipe';

describe('MurcielagoPipe', () => {
  it('create an instance', () => {
    const pipe = new MurcielagoPipe();
    expect(pipe).toBeTruthy();
  });
});
